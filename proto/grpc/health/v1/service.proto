syntax = "proto3";

package grpc.health.v1;

import "google/api/annotations.proto";

import "proto/grpc/health/v1/check.proto";

service Health {
  rpc Check(CheckRequest) returns (CheckResponse) {
    option (google.api.http) = {
      get: "/v1/health"
    };
  }
}